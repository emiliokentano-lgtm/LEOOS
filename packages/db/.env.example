# Point at a Postgres 16 instance. Never commit a real .env (engineering rule 17).
DATABASE_URL=postgres://leoos:leoos@localhost:5432/leoos

# Optional
DATABASE_POOL_MAX=10
DATABASE_STATEMENT_TIMEOUT_MS=15000
DATABASE_SSL=false
